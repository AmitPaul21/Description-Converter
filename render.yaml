services:
  - type: web
    name: description-converter
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run conmachine.py --server.port $PORT --server.address 0.0.0.0
